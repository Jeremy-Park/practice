import './PlayerName.css';

import React from 'react'

const PlayerName = ({playerName}) => {
    return (
        <div>
            <h1>{playerName}</h1>
        </div>
    )
}

export default PlayerName
